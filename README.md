# Docker 部署 （推荐）
```
cd /var/www/muchat

git clone muchat_aio
git clone muchat_go
git clone muchat_react

cd muchat_aio
docker compose up
```

- 访问 Web UI： http://localhost:3000
- 访问 管理后台： http://localhost/admin